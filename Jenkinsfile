pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent Yourself, after some changes...'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
