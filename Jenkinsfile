pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent Yourself, Maybe this is the last changes...'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
